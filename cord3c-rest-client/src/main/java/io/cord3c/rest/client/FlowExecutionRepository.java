package io.cord3c.rest.client;

import io.crnk.core.repository.ResourceRepository;

import java.util.UUID;

public interface FlowExecutionRepository extends ResourceRepository<FlowExecutionDTO, UUID> {
}
